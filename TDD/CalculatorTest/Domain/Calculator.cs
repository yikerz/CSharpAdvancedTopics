namespace Domain
{
    // 2. Create new class library project to the solution
    public class Calculator
    {
        // 3. Create Sum method
        public int Sum(int left, int right)
        {
            return left + right;
        }
    }
}
